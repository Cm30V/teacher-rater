
---

### 📝 `teacher-rater` README

```markdown
# Teacher Rater

A simple Java console application for students to rate their teachers and view average ratings.

## Features
- Add teacher ratings (1–5 scale)
- View average ratings per teacher
- Persist ratings in a local CSV file (`rating.csv`)
- Object-oriented design using a custom `Teacher` class

## How to Run
1. Compile the project:
